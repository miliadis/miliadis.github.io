<!DOCTYPE html>
<html>

    {% include head.html %}

    <body id="page-top" class="index">

        {% include nav.html %}
<section>
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2>Publications</h2>
                <hr class="star-primary">
            </div>
        </div>
        <div class="row" > 
            {% for post in site.posts %}
            {% unless post.draft %}
            <div class="container" style="padding-bottom: 30px;">	

                <div class="col-sm-4 portfolio-item" align="center">
                    <img src="img/publications/{{ post.img }}" class="img-responsive" style="width: 233px; height: 180px;">
                </div>			 
                <h4 class="title"><a href="{{ post.url }}" rel="prefetch">{{ post.title }}</a></h4>
                <p>{{ post.authors }}</p>
                <div class="meta" style="padding-bottom: 5px;"><i>{{ post.booktitle }}</i></div>	

                <a  href="{{ post.paper }}">Paper</a> /
                <a  href="{{ post.url }}">Project Page</a>
                {% if post.code %} / <a  href="{{ post.code }}">Code</a>{% endif %}

            </div>	

            {% endunless %}
            {% endfor %}
        </div>
    </div>
</section>

        {% include footer.html %}    
        {% include js.html %}

    </body>
</html>
